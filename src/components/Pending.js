import React from "react";
import { Icon } from "semantic-ui-react";

export default () => (
  <div style={{ textAlign: "center", fontSize: "3em" }}>
    <Icon name="hourglass half" color="green" circular />
  </div>
);
